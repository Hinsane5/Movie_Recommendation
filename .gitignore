# --- Large raw dataset & build artifacts (NEVER commit: exceed GitHub's 100MB limit) ---
archive/
archive.zip
*.joblib
mymoviedb.csv

# The committed catalogue (data/movies.csv.gz) and the precomputed collaborative-filtering
# neighbour table (data/cf_neighbors.csv.gz) are kept — the deployed app loads them at
# runtime. Ignore any uncompressed dumps.
data/*.csv
data/*.csv.gz
!data/movies.csv.gz
!data/cf_neighbors.csv.gz

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# --- Tooling / OS ---
.DS_Store
.streamlit/secrets.toml

# --- Legacy Flask app (superseded by streamlit_app.py) ---
app.py
templates/
static/
Procfile
__pycache__/
